services:
  doc:
    image: guergeiro/pnpm:22-10-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "pnpm install && pnpm run dev"
    environment:
      - HOST=0.0.0.0